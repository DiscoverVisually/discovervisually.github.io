# Step Into Wonder

Single-page immersive parallax landing page built with React, TypeScript, Tailwind CSS, and Vite.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Core implementation is in `src/App.tsx`. Tailwind is used only for responsive breakpoint layout classes; visual styling is inline React `CSSProperties`.
